Run these commands to setup environment:

```gitbash
git clone https://github.com/your-username/task2-animation-agent.git
cd task2-animation-agent
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
```
